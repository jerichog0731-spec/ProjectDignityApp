# Copy to .env.local and fill in values. Never commit .env.local.

AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_CLIENTS_TABLE=Clients
AIRTABLE_TRANSACTIONS_TABLE=Transactions

# Server-only: validated in API routes, not exposed to the client bundle.
ADMIN_PIN=
